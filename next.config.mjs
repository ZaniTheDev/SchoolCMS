/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: false,
  images: {
    domains: ["i.pinimg.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
